{% extends "base.jinja2" %}
{% block content %}
<h1>Welcome to my Blog!</h1>
{% for post in posts %}
<div class="post-preview">
    <div class="card">
        <div class="card-body">
            <a class="card-title" href="{{ url_for('blog.post', id=post.id) }}">
                <h5>{{ post.title }}</h5>
            </a>
            <h6 class="card-subtitle mb-2 text-muted">{{ post.timestamp.strftime('%m/%d/%Y, %I:%M:%S %p') }}</h6>
            <p class="card-text">{{ post.preview }}</p>
            {% for category in post.categories %}
                <a href="{{ url_for('blog.postsCategory', category=category.name) }}" class="btn btn-info btn-sm card-link">{{ category.name.capitalize() }}</a>
            {% endfor %}
        </div>
    </div>
</div>
{% endfor %}
{% endblock %}